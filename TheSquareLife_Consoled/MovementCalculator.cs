using TheSquareLife_Consoled.Entities;

namespace TheSquareLife_Consoled;

internal static class MovementCalculator
{
    public static List<Position> PossibleMoveCoordinates(Entity entity, Board board)
    {
        var list = new List<Position>();
        
        // move up position
        //var upperCoordinates = Entity.Position.
        
        return list;
    }
}